#group-log-in-or-join.modal.hide
  .modal-header
    %button{:type=>"button", :class=>"close", :"data-dismiss"=>"modal"}
      ×
    %h3
      Have you already got an account?
  .modal-body
    %p="If you already have an account then #{link_to('log in here', sign_in_path(:next => return_to))}.".html_safe
    %p Otherwise click 'Next' to continue.
  .modal-footer
    =link_to "Next", group_join_path, :class => "cta-intro"