#conditions-popup.modal.hide.fade
  .modal-header
    %button{:type=>"button", :class=>"close", :"data-dismiss"=>"modal"}
      ×
    %h3
      =Page.find_by_slug(:conditions).title
  .modal-body  
    =simple_format(Page.find_by_slug(:conditions).text)
  .modal-footer
    %button{:type=>"button", :class=>"btn btn-primary", :"data-dismiss"=>"modal"}
      Close